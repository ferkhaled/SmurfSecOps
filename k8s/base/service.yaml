apiVersion: v1
kind: Service
metadata:
  name: berryshop-api
  labels:
    app.kubernetes.io/name: berryshop-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: berryshop-api
  ports:
    - name: http
      port: 80
      targetPort: http

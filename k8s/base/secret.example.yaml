apiVersion: v1
kind: Secret
metadata:
  name: berryshop-api-secrets
type: Opaque
stringData:
  # Copy this file to a real secret manifest only in a safe lab context.
  # Do not commit real credentials to Git.
  API_TOKEN: change-me
